name: test-api

on:
  pull_request:
    types: [opened, reopened, synchronize]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  unit:
    name: unit
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup
      - name: Run unit tests
        run: pnpm --filter @hono-poc/api exec vitest run --project unit --coverage --reporter=dot --reporter=blob --outputFile.blob=.vitest-reports/unit.json
      - name: Upload blob report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: blob-unit
          path: apps/api/.vitest-reports/*
          include-hidden-files: true
          retention-days: 1
  integration:
    name: integration
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup
      - name: Run integration tests
        run: pnpm --filter @hono-poc/api exec vitest run --project integration --coverage --reporter=dot --reporter=blob --outputFile.blob=.vitest-reports/integration.json
      - name: Upload blob report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: blob-integration
          path: apps/api/.vitest-reports/*
          include-hidden-files: true
          retention-days: 1
  api:
    name: api
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup
      - name: Run API tests
        run: pnpm --filter @hono-poc/api exec vitest run --project api --coverage --reporter=dot --reporter=blob --outputFile.blob=.vitest-reports/api.json
      - name: Upload blob report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: blob-api
          path: apps/api/.vitest-reports/*
          include-hidden-files: true
          retention-days: 1
  coverage:
    name: coverage
    needs: [unit, integration, api]
    if: ${{ !cancelled() }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      pull-requests: write  # カバレッジレポートを PR にコメントするため
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup
      - name: Download blob reports
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          path: apps/api/.vitest-reports
          pattern: blob-*
          merge-multiple: true
      - name: Merge coverage
        run: pnpm --filter @hono-poc/api exec vitest run --merge-reports --coverage
      - name: Report coverage
        if: always()
        uses: davelosert/vitest-coverage-report-action@3c054a2d2e2ca45446417ad5d6d5eb33092af8f1  # v2.12.1
        with:
          working-directory: ./apps/api
          name: api
