# Storybook

Storybook で UI コンポーネントのカタログを表示する。

## 起動

```sh
mise run storybook:dev
```

### ブラウザで表示

```sh
mise run view:storybook
```

## ビルド

開発ドキュメントに同梱する静的ファイル(`docs/public/storybook`)を生成する。

```sh
mise run storybook:build
```
